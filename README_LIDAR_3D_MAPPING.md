# LIDAR 3D Mapping - Сканирование карты в реальном времени

Программа для 3D картографирования с помощью Lidar в симуляторе ProjectAirSim.
Запускает проект Blocks в Unreal Engine 4 и показывает 3D визуализацию облака точек 
Lidar в отдельном окне, накапливая карту сканирования в мировых координатах.

## Требования

- Python 3.8+
- Unreal Engine 4.27+ с проектом Blocks
- ProjectAirSim (установлен в проекте)
- Библиотеки Python:
  - `open3d` - 3D визуализация
  - `matplotlib` - цветовые палитры
  - `numpy` - математические операции
  - `pynng` - коммуникация с симулятором
  - `msgpack` - сериализация данных

## Установка

1. Убедитесь, что Python 3.8+ установлен и добавлен в PATH.

2. Установите зависимости:
```bash
pip install open3d matplotlib numpy pynng msgpack cryptography
```

3. Установите клиент ProjectAirSim:
```bash
cd client/python/projectairsim
pip install -e .
```

## Запуск

### Способ 1: Через bat-файл (рекомендуется)

Просто запустите `run_lidar_3d_mapping.bat` - он автоматически:
- Проверит и установит необходимые пакеты
- Активирует виртуальное окружение (если есть)
- Запустит программу

### Способ 2: Вручную

1. Запустите Unreal Engine с проектом Blocks:
   ```
   unreal/Blocks/Blocks.uproject
   ```

2. В UE4 нажмите кнопку "Play" для запуска симуляции.

3. Запустите Python скрипт:
   ```bash
   python lidar_3d_mapping.py
   ```

## Режимы работы

### 1. Автоматическое сканирование
Дрон выполняет заранее запрограммированный маршрут для полного сканирования области:
- Квадратная спираль на разных высотах
- Диагональные проходы
- Автоматическая посадка

### 2. Ручное управление
Управляйте дроном с клавиатуры в реальном времени:
- `W/S` - движение вперёд/назад
- `A/D` - движение влево/вправо
- `R/F` - подъём/снижение
- `Q/E` - поворот влево/вправо
- `C` - очистить накопленную карту
- `P` - сохранить карту в файл
- `X` - выход из режима

### 3. Режим наблюдения
Дрон поднимается и медленно вращается, сканируя окружение на 360°.

## Визуализация

В отдельном окне Open3D отображается 3D облако точек:
- Точки раскрашены по высоте (тёплые цвета - низ, холодные - верх)
- Камеру можно вращать мышью
- Координатные оси показывают ориентацию
- Карта накапливается в мировых координатах

### Управление камерой в Open3D:
- **Левая кнопка мыши** - вращение
- **Средняя кнопка / колёсико** - масштабирование
- **Правая кнопка мыши** - перемещение

## Сохранение результатов

После завершения сканирования карта автоматически сохраняется в файл:
- `lidar_map_blocks.ply` - формат PLY (Point Cloud Library)

Этот файл можно открыть в:
- MeshLab
- CloudCompare
- Open3D
- Blender (с плагином)

## Конфигурация

### Настройки Lidar

Файл `client/python/example_user_scripts/sim_config/robot_quadrotor_lidar_mapping.jsonc`:
- `number-of-channels`: 32 - количество лучей
- `range`: 150м - дальность сканирования
- `points-per-second`: 300000 - плотность точек
- `horizontal-fov`: 0-360° - полный обзор
- `vertical-fov`: -25° до +15° - вертикальный угол

### Настройки визуализации

В классе `Lidar3DMapper`:
- `max_points`: максимум точек для накопления (по умолчанию 1 000 000)
- `point_size`: размер точек в визуализации
- `accumulate_map`: накапливать карту или показывать только текущий скан
- `color_by_height`: раскрашивать по высоте или по сегментации

## Архитектура

```
lidar_3d_mapping.py
├── Lidar3DMapper - Визуализация в отдельном процессе
│   ├── display_loop() - цикл рендеринга Open3D
│   ├── receive() - получение данных с Lidar
│   └── transform_to_world() - преобразование в мировые координаты
│
└── BlocksLidarScanner - Управление симуляцией
    ├── start_blocks() - запуск UE4
    ├── run() - основной цикл
    ├── _auto_scan_mode() - автоматическое сканирование
    ├── _manual_control_mode() - ручное управление
    └── _observation_mode() - режим наблюдения
```

## Troubleshooting

### Ошибка подключения к симулятору
- Убедитесь, что UE4 с Blocks запущен и нажата кнопка Play
- Проверьте, что порты 8989 и 8990 не заняты

### Окно Open3D не открывается
- Установите последнюю версию open3d: `pip install open3d --upgrade`
- На Windows может потребоваться Visual C++ Redistributable

### Низкая производительность
- Уменьшите `max_points` в настройках
- Уменьшите `points-per-second` в конфигурации Lidar
- Используйте режим без накопления карты (`accumulate_map=False`)

## Лицензия

MIT License
Copyright (C) 2025

---

Приятного сканирования! 🚁📡
