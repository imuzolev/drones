# Полное описание репозитория ProjectAirSim

## 🎯 Общая информация

**ProjectAirSim** — это платформа симуляции для дронов, роботов и других автономных систем, основанная на продолжении разработки Microsoft AirSim. Проект использует **Unreal Engine 5.2** для фотореалистичной визуализации и предоставляет фреймворк для интеграции физики, контроллеров, приводов и датчиков.

В данном репозитории реализована **система 3D картографирования с помощью LiDAR**, которая позволяет:
- Запускать дрона в симуляторе Unreal Engine
- Собирать данные LiDAR в реальном времени
- Визуализировать облако точек в отдельном окне Open3D
- Накапливать 3D карту в мировых координатах
- Сохранять результаты сканирования в формате PLY

---

## 📁 Структура репозитория

### Основные директории:

```
ProjectAirSim/
├── 📄 README.md                           # Основная документация проекта
├── 📄 README_LIDAR_3D_MAPPING.md          # Документация по LiDAR картографированию
├── 📄 lidar_3d_mapping.py                 # ГЛАВНЫЙ СКРИПТ - программа 3D картографирования
├── 📄 run_lidar_3d_mapping.bat            # Batch-файл для запуска на Windows
│
├── 📁 unreal/Blocks/                      # Проект Unreal Engine
│   ├── Blocks.uproject                    # Файл проекта UE5.2
│   ├── Config/                            # Конфигурационные файлы
│   ├── Content/                           # Контент сцены (геометрия, материалы)
│   └── Source/                            # Исходный код C++
│
├── 📁 client/python/                      # Python клиентская библиотека
│   ├── projectairsim/                     # Основная библиотека ProjectAirSim
│   │   └── src/projectairsim/             # Исходный код API
│   └── example_user_scripts/              # Примеры использования
│       └── sim_config/                    # Конфигурационные файлы JSONC
│           ├── robot_quadrotor_lidar_mapping.jsonc   # Конфиг дрона с LiDAR
│           └── scene_blocks_lidar_mapping.jsonc      # Конфиг сцены Blocks
│
├── 📁 core_sim/                           # Ядро симуляции (C++)
├── 📁 physics/                            # Физический движок
├── 📁 vehicle_apis/                       # API для управления роботами
├── 📁 docs/                               # Документация
└── 📁 venv/                               # Виртуальное окружение Python (если создано)
```

---

## 🔧 Ключевые файлы и их назначение

### 1. `lidar_3d_mapping.py` (763 строки)
**Главный файл программы** для 3D картографирования. Содержит:

#### Класс `Lidar3DMapper`:
- **Назначение**: Визуализация облака точек LiDAR в отдельном процессе
- **Функции**:
  - `start()` - запуск визуализации в отдельном окне Open3D
  - `receive(lidar_data)` - получение и обработка данных LiDAR
  - `transform_to_world()` - преобразование точек из координат сенсора в мировые
  - `clear_map()` - очистка накопленной карты
  - `save_map()` - сохранение карты в файл PLY
- **Параметры**:
  - `max_points`: максимум точек (по умолчанию 1,000,000)
  - `point_size`: размер точек в визуализации
  - `accumulate_map`: накапливать карту или показывать только текущий скан
  - `color_by_height`: раскрашивать по высоте или по сегментации

#### Класс `BlocksLidarScanner`:
- **Назначение**: Управление симулятором и дроном
- **Функции**:
  - `start_blocks()` - автоматический запуск UE5 (если найден исполняемый файл)
  - `run()` - основной цикл программы
  - `_auto_scan_mode()` - автоматическое сканирование по маршруту
  - `_manual_control_mode()` - ручное управление с клавиатуры
  - `_observation_mode()` - режим наблюдения (дрон вращается на месте)

### 2. `run_lidar_3d_mapping.bat`
**Batch-скрипт для Windows**, который:
- Проверяет наличие Python
- Активирует виртуальное окружение (если есть)
- Устанавливает необходимые пакеты (`open3d`, `matplotlib`, `numpy`, `pynng`, `msgpack`, `cryptography`)
- Устанавливает библиотеку `projectairsim`
- Запускает `lidar_3d_mapping.py`

### 3. `robot_quadrotor_lidar_mapping.jsonc`
**Конфигурация дрона** с настройками:
- **Физика**: fast-physics (быстрая физика для квадрокоптера)
- **Квадрокоптер**: 4 пропеллера (Prop_FL, Prop_FR, Prop_RL, Prop_RR)
- **Контроллер**: Simple_Flight_Controller
- **Сенсоры**:
  - **LiDAR**: `lidar1` - основной датчик
    - 32 канала
    - Дальность: 150м
    - 300,000 точек в секунду
    - Горизонтальный FOV: 0-360° (полный обзор)
    - Вертикальный FOV: -25° до +15°
  - **Камера**: `Chase` - камера преследования для обзора
  - **IMU**: `IMU1` - инерциальный измерительный блок
  - **GPS**: глобальное позиционирование

### 4. `scene_blocks_lidar_mapping.jsonc`
**Конфигурация сцены**:
- Название: `SceneBlocksLidarMapping`
- Робот: `Drone1` в позиции (0, 0, -3)
- Частота обновления: 3ms шаг симуляции
- Географическая точка: Сиэтл (47.641468, -122.140165, 122м)
- Сегментация: включена для классификации объектов

---

## 🚀 Пошаговая инструкция по использованию

### Шаг 1: Предварительные требования

#### 1.1 Установка Python
- **Требуется**: Python 3.8 - 3.11 (рекомендуется 3.11)
- ⚠️ **Важно**: Python 3.13 не полностью поддерживается Open3D
- Если у вас Python 3.13, создайте виртуальное окружение с Python 3.11:
  ```powershell
  py -3.11 -m venv venv311
  venv311\Scripts\activate
  ```

#### 1.2 Установка Unreal Engine 5.2
- Скачайте и установите [Unreal Engine 5.2](https://www.unrealengine.com/en-US/download)
- Установите переменную окружения `UE_ROOT` (опционально)
- Убедитесь, что проект Blocks можно открыть в UE5.2

#### 1.3 Установка зависимостей Python
```powershell
pip install open3d matplotlib numpy pynng msgpack cryptography
```

#### 1.4 Установка клиентской библиотеки ProjectAirSim
```powershell
cd client\python\projectairsim
pip install -e .
cd ..\..\..
```

---

### Шаг 2: Подготовка проекта Blocks

#### 2.1 Сборка проекта (если еще не собран)

**Для Windows (в "x64 Native Tools Command Prompt for VS 2022"):**
```cmd
build.cmd simlibs_debug
blocks_genprojfiles_vscode.bat
```

**Для Linux:**
```bash
./build.sh simlibs_debug
./blocks_genprojfiles_vscode.sh
```

#### 2.2 Открытие проекта в Unreal Engine
1. Откройте `unreal\Blocks\Blocks.uproject` в Unreal Engine 5.2
2. Если появится запрос на перекомпиляцию - согласитесь
3. Дождитесь завершения компиляции и загрузки проекта

---

### Шаг 3: Запуск симулятора

**Вариант A: Автоматический запуск (если есть скомпилированный .exe)**
- Скрипт сам найдет и запустит `Blocks.exe` (если находится в стандартном месте)

**Вариант B: Ручной запуск**
1. Откройте `unreal\Blocks\Blocks.uproject` в Unreal Engine
2. В редакторе нажмите кнопку **"Play"** (▶️) для запуска симуляции
3. Дождитесь полной загрузки сцены

---

### Шаг 4: Запуск программы картографирования

#### Способ 1: Через batch-файл (рекомендуется)
```powershell
run_lidar_3d_mapping.bat
```

#### Способ 2: Вручную через Python
```powershell
# Активируйте виртуальное окружение (если используется)
venv\Scripts\activate

# Запустите скрипт
python lidar_3d_mapping.py
```

---

### Шаг 5: Выбор режима работы

После запуска программа предложит выбрать режим:

#### **Режим 1: Автоматическое сканирование** (рекомендуется для первого запуска)
```
Выберите режим:
  1 - Автоматическое сканирование
  2 - Ручное управление
  3 - Режим наблюдения
Введите номер: 1
```

**Что происходит:**
- Дрон автоматически взлетает
- Выполняет запрограммированный маршрут:
  - Квадратная спираль на разных высотах
  - Диагональные проходы для полного покрытия
  - Автоматическая посадка после завершения
- Карта накапливается автоматически

#### **Режим 2: Ручное управление**
```
Введите номер: 2
```

**Управление с клавиатуры:**
- `W` / `S` - движение вперед/назад
- `A` / `D` - движение влево/вправо
- `R` / `F` - подъем/снижение
- `Q` / `E` - поворот влево/вправо
- `C` - очистить накопленную карту
- `P` - сохранить карту в файл
- `X` - выход из режима

#### **Режим 3: Режим наблюдения**
```
Введите номер: 3
```

**Что происходит:**
- Дрон поднимается на заданную высоту
- Медленно вращается на 360°, сканируя окружение
- Подходит для быстрого обзора местности

---

### Шаг 6: Визуализация облака точек

После запуска открывается окно **Open3D** с визуализацией:

**Отображение:**
- 🟦 Точки раскрашены по высоте (цветовая палитра "plasma")
  - Тёплые цвета (жёлтый, оранжевый) = низкие точки
  - Холодные цвета (синий, фиолетовый) = высокие точки
- 📐 Координатные оси показывают ориентацию мира
- 🔄 Карта накапливается в реальном времени

**Управление камерой в окне Open3D:**
- **Левая кнопка мыши + перетаскивание** - вращение камеры
- **Средняя кнопка / колёсико мыши** - масштабирование (zoom)
- **Правая кнопка мыши + перетаскивание** - перемещение камеры
- **R** - сброс вида камеры

---

### Шаг 7: Сохранение результатов

**Автоматическое сохранение:**
- После завершения автоматического сканирования карта сохраняется в `lidar_map_blocks.ply`

**Ручное сохранение:**
- В режиме ручного управления нажмите `P`
- Или в коде программы: `mapper.save_map("my_map.ply")`

**Открытие файла PLY:**
- **MeshLab** - бесплатный редактор облаков точек
- **CloudCompare** - продвинутый инструмент обработки облаков точек
- **Blender** (с плагином для PLY)
- **Open3D** (программно)

---

## 🔍 Детальный разбор работы программы

### Процесс работы (последовательность):

1. **Инициализация клиента**
   ```python
   client = ProjectAirSimClient()
   client.connect()  # Подключение к симулятору через сеть (порты 8989, 8990)
   ```

2. **Загрузка сцены**
   ```python
   world = World(client, "scene_blocks_lidar_mapping.jsonc", delay_after_load_sec=3)
   # Загружает конфигурацию сцены из JSONC файла
   ```

3. **Создание объекта дрона**
   ```python
   drone = Drone(client, world, "Drone1")
   drone.enable_api_control()  # Разрешает программное управление
   drone.arm()  # Активирует двигатели
   ```

4. **Подписка на данные LiDAR**
   ```python
   client.subscribe(
       drone.sensors["lidar1"]["lidar"],
       lambda _, lidar: mapper.receive(lidar)  # Callback функция
   )
   ```

5. **Запуск визуализации**
   ```python
   mapper.start()  # Запускает процесс Open3D
   ```

6. **Основной цикл**
   - Дрон выполняет движения (автоматически или вручную)
   - LiDAR собирает точки
   - Данные передаются в визуализатор через очередь `multiprocessing.Queue`
   - Точки преобразуются в мировые координаты
   - Отображаются в окне Open3D

---

## ⚙️ Настройка параметров

### Изменение параметров LiDAR

Отредактируйте `client/python/example_user_scripts/sim_config/robot_quadrotor_lidar_mapping.jsonc`:

```jsonc
{
  "sensors": [{
    "id": "lidar1",
    "number-of-channels": 32,      // Количество лучей (16, 32, 64)
    "range": 150,                   // Дальность в метрах
    "points-per-second": 300000,    // Плотность точек
    "horizontal-fov-start-deg": 0.0,
    "horizontal-fov-end-deg": 360.0,  // 360° = полный обзор
    "vertical-fov-upper-deg": 15.0,
    "vertical-fov-lower-deg": -25.0
  }]
}
```

### Изменение параметров визуализации

В файле `lidar_3d_mapping.py`, класс `Lidar3DMapper.__init__()`:

```python
self.mapper = Lidar3DMapper(
    win_name="LIDAR 3D Карта - Blocks",
    width=1280,              # Ширина окна
    height=720,              # Высота окна
    max_points=1000000,      # Максимум точек для накопления
    point_size=2.0,          # Размер точки в пикселях
    accumulate_map=True,     # Накапливать карту
    color_by_height=True,    # Раскраска по высоте
)
```

### Изменение маршрута автоматического сканирования

В методе `_auto_scan_mode()` класса `BlocksLidarScanner`:

```python
# Пример изменения высоты полёта
await self.drone.move_to_position_async(x, y, -10, velocity=5)  # z = -10 (высота 10м)
```

---

## 🐛 Решение проблем (Troubleshooting)

### Проблема 1: "Ошибка подключения к симулятору"

**Причины:**
- UE5 не запущен или не нажата кнопка Play
- Порты 8989 и 8990 заняты другими процессами
- Неправильный путь к конфигурационным файлам

**Решения:**
1. Убедитесь, что UE5 запущен и нажата кнопка **Play**
2. Проверьте, что конфигурационные файлы находятся в правильной директории
3. Закройте другие программы, использующие эти порты

### Проблема 2: "Open3D не устанавливается"

**Причина:** Python 3.13 не полностью поддерживается

**Решение:**
```powershell
# Создайте виртуальное окружение с Python 3.11
py -3.11 -m venv venv311
venv311\Scripts\activate
pip install open3d matplotlib numpy pynng msgpack cryptography
```

### Проблема 3: "Окно Open3D не открывается"

**Решения:**
1. Обновите Open3D: `pip install open3d --upgrade`
2. Установите Visual C++ Redistributable (Windows)
3. Проверьте, что видеодрайверы обновлены

### Проблема 4: "Низкая производительность / лаги"

**Решения:**
- Уменьшите `max_points` в настройках визуализации (например, до 500,000)
- Уменьшите `points-per-second` в конфигурации LiDAR (например, до 100,000)
- Используйте режим без накопления: `accumulate_map=False`
- Закройте другие ресурсоёмкие программы

### Проблема 5: "Дрон не взлетает / падает"

**Решения:**
1. Проверьте, что вызваны `drone.enable_api_control()` и `drone.arm()`
2. Убедитесь, что физика симулятора работает корректно
3. Проверьте начальную позицию дрона в конфигурации сцены

---

## 📊 Формат данных

### Структура данных LiDAR

Каждый кадр LiDAR содержит:
```python
{
    "timestamp": 1234567890,  # Время в наносекундах
    "point_cloud": [          # Массив точек
        [x1, y1, z1],         # Координаты в системе сенсора
        [x2, y2, z2],
        ...
    ],
    "pose": {                 # Поза сенсора
        "position": {"x": 0.0, "y": 0.0, "z": -3.0},
        "orientation": {"w": 1.0, "x": 0.0, "y": 0.0, "z": 0.0}  # Кватернион
    }
}
```

### Формат сохранения PLY

Файл PLY содержит:
- Заголовок с метаданными
- Список точек (x, y, z, r, g, b) - координаты и цвета

---

## 🎓 Примеры использования

### Пример 1: Простое сканирование с сохранением

```python
import asyncio
from lidar_3d_mapping import BlocksLidarScanner

async def main():
    scanner = BlocksLidarScanner()
    scanner.start_blocks()  # Запустить UE5
    
    await scanner.run()  # Запустить основной цикл
    
    # После завершения карта автоматически сохранится

asyncio.run(main())
```

### Пример 2: Настройка кастомных параметров

```python
from lidar_3d_mapping import Lidar3DMapper

mapper = Lidar3DMapper(
    max_points=2000000,      # Больше точек
    point_size=1.5,          # Меньше размер точек
    accumulate_map=True,
    color_by_height=False    # Раскраска по сегментации
)
```

---

## 📚 Дополнительные ресурсы

### Документация ProjectAirSim
- Основная документация: `docs/`
- API справочник: `docs/api.md`
- Конфигурация: `docs/config.md`

### Примеры использования
- Базовые примеры: `client/python/example_user_scripts/`
- Примеры с LiDAR: 
  - `lidar_basic.py`
  - `lidar_livox.py`

### Поддержка
- Discord: https://discord.gg/XprQ2w64uj
- GitHub Discussions: https://github.com/iamaisim/ProjectAirSim/discussions

---

## 🔐 Системные требования

### Минимальные требования:
- **ОС**: Windows 11 или Ubuntu 22.04
- **CPU**: 4 ядра, 3.0 GHz
- **RAM**: 16 GB
- **GPU**: DirectX 11/12 совместимая видеокарта с 4 GB VRAM
- **Python**: 3.8 - 3.11
- **Unreal Engine**: 5.2

### Рекомендуемые требования:
- **CPU**: 8+ ядер, 3.5+ GHz
- **RAM**: 32 GB
- **GPU**: NVIDIA RTX 3060 или лучше с 8+ GB VRAM
- **Диск**: SSD с 50+ GB свободного места

---

## 📝 Лицензия

MIT License  
Copyright (C) Microsoft Corporation  
Copyright (C) 2025 IAMAI CONSULTING CORP

---

## 🎯 Краткое резюме

**Что делает этот репозиторий:**
1. ✅ Предоставляет платформу симуляции ProjectAirSim на базе Unreal Engine 5.2
2. ✅ Реализует систему 3D картографирования с LiDAR для дронов
3. ✅ Визуализирует облако точек в реальном времени через Open3D
4. ✅ Сохраняет 3D карты в формате PLY для дальнейшего использования

**Как использовать:**
1. Установите Python 3.11, Unreal Engine 5.2, зависимости
2. Запустите `run_lidar_3d_mapping.bat`
3. Выберите режим работы (автоматический/ручной/наблюдение)
4. Наблюдайте за построением 3D карты в окне Open3D
5. Результаты автоматически сохраняются в PLY файл

---

*Последнее обновление: 2025*
